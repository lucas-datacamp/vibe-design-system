# build output
dist/
# generated types
.astro/

# generated agent layer (regenerated from global.css on every dev/build)
public/tokens.json
public/tokens.css
public/system.md
public/llms.txt
public/v1/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

# impeccable live runtime (config.json is committed)
.impeccable/live/server.json
.impeccable/live/sessions/

# impeccable harness installs (local per machine)
.agents/
.gemini/
.opencode/

# Harness dirs ignored, EXCEPT our two shipped skills (must be present on clone).
.claude/*
!.claude/skills/
.claude/skills/*
!.claude/skills/apply-system/
!.claude/skills/publish-system/
.cursor/*
!.cursor/skills/
.cursor/skills/*
!.cursor/skills/apply-system/
!.cursor/skills/publish-system/

# personal design context (created during the session, one per attendee)
PRODUCT.md
DESIGN.md

# session planning / decisions (local, not shipped to attendees)
SESSION-PLAN.md

# jetbrains setting folder
.idea/
